// The main component page to create the normalize view of the index page
import { Component }     from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'my-app',
    templateUrl: './views/app.component.html'
})

export class AppComponent { }